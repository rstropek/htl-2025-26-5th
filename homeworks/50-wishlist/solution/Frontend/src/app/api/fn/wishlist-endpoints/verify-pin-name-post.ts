/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { VerifyPinRequestDto } from '../../models/verify-pin-request-dto';
import { VerifyPinResponseDto } from '../../models/verify-pin-response-dto';

export interface VerifyPinNamePost$Params {
  name: string;
      body: VerifyPinRequestDto
}

export function verifyPinNamePost(http: HttpClient, rootUrl: string, params: VerifyPinNamePost$Params, context?: HttpContext): Observable<StrictHttpResponse<VerifyPinResponseDto>> {
  const rb = new RequestBuilder(rootUrl, verifyPinNamePost.PATH, 'post');
  if (params) {
    rb.path('name', params.name, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<VerifyPinResponseDto>;
    })
  );
}

verifyPinNamePost.PATH = '/verify-pin/{name}';
