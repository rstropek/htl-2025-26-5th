/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { TimeEntryDto } from '../../models/time-entry-dto';

export interface TimeentriesGet$Params {
  employeeId?: number;
  projectId?: number;
}

export function timeentriesGet(http: HttpClient, rootUrl: string, params?: TimeentriesGet$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<TimeEntryDto>>> {
  const rb = new RequestBuilder(rootUrl, timeentriesGet.PATH, 'get');
  if (params) {
    rb.query('employeeId', params.employeeId, {});
    rb.query('projectId', params.projectId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<TimeEntryDto>>;
    })
  );
}

timeentriesGet.PATH = '/timeentries';
